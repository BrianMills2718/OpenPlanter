# acceptance_gates

Acceptance gates for `OpenPlanter`.

Keep this subtree focused on machine-checkable exit criteria for plans and
feature slices. Gates here should define what must be proven before shipping,
not restate implementation details that belong in `docs/plans/` or `agent/`.
