# agent

Runtime implementation for the `OpenPlanter` investigative agent.

Edit this subtree when changing prompt assembly, tool routing, runtime state,
credential handling, or the terminal/TUI workflow. Keep operational policy in
repo root or `docs/`; keep package-local behavioral contracts here.
