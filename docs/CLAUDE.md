# docs

Repo-local documentation for `OpenPlanter`.

## Route By Question

- architecture or decision records -> `adr/`
- active implementation planning and gap tracking -> `plans/`

## Working Rules

- Keep this parent file at routing level only.
- Put plan-index rules in `plans/` and architecture decisions in `adr/`.
- Use `docs/` for maintained operator documentation, not runtime scratch state.
